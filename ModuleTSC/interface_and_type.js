"use strict";
const printS = (coord) => {
};
const some = (point) => {
    const d = point; // Каст(явное приведение интерфейса родителя к интерфейс наследника)
};
const myCanvas = document.getElementById('canvas'); // Явное приведение к типу
